import React from "react";
import { styled, Box } from "@mui/material";

const RightFrame = styled(Box)(({ theme }) => ({
  width: "50%",
  position: "relative",
  zIndex: 2,
}));

export default RightFrame;
